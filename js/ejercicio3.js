let i = prompt("ingresa un numero del 1 al 100")

for(multiplo = 1; multiplo <= 1000; multiplo++) 
    {
      console.log(i * multiplo)   
    
    let resultado = i * multiplo
    if (resultado >= 1000){break}}